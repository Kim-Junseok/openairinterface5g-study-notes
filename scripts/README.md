# Scripts

Small helper scripts can live here later, for example:

- source baseline refreshers,
- local link checkers,
- diagram generation helpers,
- log parsing helpers.

Keep scripts optional. The notes should remain readable without running custom tooling.

